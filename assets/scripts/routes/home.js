import LogoChanger from './home/Logo';

export default {
    init() {
        // Javascript that fires on home.
        LogoChanger();
    },

    finalize() {
        // Javascript that fires on home. after page specific JS is fires.
    }
};
